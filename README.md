# IA_project

USAGE INSTRUCTIONS:
-create ".env" file inside the folder IA_project-main. In the file, write:  SUPABASE_URL = url given
                                                                            SUPABASE_KEY = key given
-run the every file
